# ============================================
# MiniLink Backend - Environment Configuration
# ============================================

# ----------------
# Server Configuration
# ----------------
PORT=3000
NODE_ENV=development
APP_URL=http://localhost:3000
FRONTEND_URL=http://localhost:5173

# ----------------
# Database
# ----------------
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/minilink?retryWrites=true&w=majority

# ----------------
# JWT Authentication
# ----------------
# IMPORTANT: Use a strong secret (minimum 32 characters)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ----------------
# CORS Configuration
# ----------------
# Comma-separated list of allowed origins
# Supports wildcards: *.vercel.app
CORS_ORIGIN=http://localhost:5173,http://localhost:3000,https://yourdomain.com

# ----------------
# Google OAuth
# ----------------
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/oauth/google/callback

# ----------------
# GitHub OAuth (Optional - for future implementation)
# ----------------
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost:3000/api/oauth/github/callback

# ----------------
# Rate Limiting (Optional - defaults applied if not set)
# ----------------
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ----------------
# Security (Optional)
# ----------------
BCRYPT_ROUNDS=12
SESSION_TIMEOUT=604800000

# ----------------
# Analytics (Optional)
# ----------------
ANALYTICS_RETENTION_DAYS=90

# ============================================
# Production Environment Notes
# ============================================
# 
# For production deployment:
# 1. Change NODE_ENV to 'production'
# 2. Use a strong JWT_SECRET (minimum 32 characters)
# 3. Update APP_URL to your production domain
# 4. Update FRONTEND_URL to your frontend domain
# 5. Set proper CORS_ORIGIN values
# 6. Use production MongoDB URI with proper credentials
# 7. Configure OAuth redirect URIs for production domain
# 8. Enable HTTPS
# 9. Set up monitoring and logging
# 10. Configure backup strategy
#
# Security Checklist:
# - JWT_SECRET: Strong random string
# - MongoDB: Use strong password and IP whitelist
# - OAuth: Secure client secrets
# - CORS: Restrict to specific domains only
# - Environment: Never commit .env to version control
#
# ============================================
